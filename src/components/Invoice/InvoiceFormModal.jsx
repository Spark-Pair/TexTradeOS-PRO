import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, ArrowRight, Edit3, Plus, RotateCcw, Save, ScanLine, Trash2 } from "lucide-react";
import { v4 as uuidv4 } from "uuid";
import { motion } from "framer-motion";
import Modal from "../Modal";
import Button from "../Button";
import Input from "../Input";
import Select from "../Select";
import Checkbox from "../Checkbox";
import { SectionHeader } from "../SectionHeader";
import { fetchMyInvoiceCounter } from "../../api/business";
import { fetchSalesReturnable } from "../../api/returns.api";
import { createCustomer, fetchCustomers, fetchInventory } from "../../api/commerce";
import { useToast } from "../../context/ToastContext";
import { InvoicePrintPreview } from "./InvoicePreviewModal";
import ReturnEditor, { returnTotals } from "../Returns/ReturnEditor";
import PaymentEditor from "./PaymentEditor";
import CustomerFormModal from "../User/CustomerFormModal";
import InvoiceScanModal, { unlockScanAudio } from "../Scanner/InvoiceScanModal";

const todayInput = () => { const d = new Date(); return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`; };
const numberValue = (value) => { const n = Number(String(value ?? "").replace(/,/g,"")); return Number.isFinite(n) ? n : 0; };
const capitalizeWords = (value) => String(value ?? "").toLowerCase().replace(/\s+/g," ").replace(/\b\w/g,(c)=>c.toUpperCase());
const formatInvoiceNumber = (year,next) => `${year}-${String(Math.max(1,Number(next)||1)).padStart(4,"0")}`;
const toInvoiceInventory = (items=[]) => items.map((item)=>({...item,value:item.article_no,stock_pcs:Math.max(0,numberValue(item.stock_pcs ?? item.available_pcs)),label:`${item.article_no} | ${item.description||"Article"} | Stock ${Math.max(0,numberValue(item.stock_pcs ?? item.available_pcs))} pcs | Sale ${item.sale_rate||item.rate||0}`})).filter((item)=>item.stock_pcs>0);
const newInvoiceArticle = (item) => ({_key:uuidv4(),article_no:item.article_no,purchase_number:item.purchase_number,description:item.description||"",size:item.size||"",unit:numberValue(item.unit),quantity_pkt:"",dzn:"",pcs:"",purchase_rate:numberValue(item.rate),rate:numberValue(item.sale_rate||item.rate),discount:item.discount||""});
const syncQuantity = (row,field,value) => { const unit=Math.max(0,numberValue(field==="unit"?value:row.unit)); const next={...row,[field]:value}; if(field==="quantity_pkt"){const pcs=numberValue(value)*unit;next.pcs=pcs?String(pcs):"";next.dzn=pcs?String(pcs/12):"";} if(field==="dzn"){const pcs=numberValue(value)*12;next.pcs=pcs?String(pcs):"";next.quantity_pkt=unit?String(pcs/unit):"";} if(field==="pcs"){const pcs=numberValue(value);next.dzn=pcs?String(pcs/12):"";next.quantity_pkt=unit?String(pcs/unit):"";} return next; };
const discountDetails = (discount,pcs,rate) => { const raw=String(discount||"").trim(); if(!raw)return{amount:0,type:""}; const pieces=Math.max(0,numberValue(pcs)),unitRate=Math.max(0,numberValue(rate)); if(!pieces||!unitRate)return{amount:0,type:raw.endsWith("%")?"percent":"rupee"}; if(raw.endsWith("%")){const p=Math.min(100,Math.max(0,numberValue(raw.slice(0,-1))));return{amount:pieces*(unitRate*p/100),type:"percent"};} return{amount:pieces*Math.min(unitRate,Math.max(0,numberValue(raw))),type:"rupee"}; };
const calculateArticle = (row) => { const dzn=numberValue(row.dzn),pcs=numberValue(row.pcs),rate=numberValue(row.rate),gross=pcs*rate,discount=discountDetails(row.discount,pcs,rate),discountAmount=Math.min(gross,discount.amount); return{...row,dzn,pcs,quantity_pkt:numberValue(row.quantity_pkt),rate,gross_amount:gross,discount_type:discount.type,discount_amount:discountAmount,amount:Math.max(0,gross-discountAmount)}; };
const calculateInvoiceTotals = (rows,returnAmount=0,receivedAmount=0) => { const gross_amount=rows.reduce((s,r)=>s+Number(r.gross_amount||0),0),percent_discount_amount=rows.reduce((s,r)=>s+(r.discount_type==="percent"?Number(r.discount_amount||0):0),0),rupee_discount_amount=rows.reduce((s,r)=>s+(r.discount_type==="rupee"?Number(r.discount_amount||0):0),0),total_discount_amount=percent_discount_amount+rupee_discount_amount,net_amount=Math.max(0,gross_amount-total_discount_amount),sales_return_amount=Math.min(net_amount,Math.max(0,numberValue(returnAmount))),total_amount=Math.max(0,net_amount-sales_return_amount),received_amount=Math.max(0,numberValue(receivedAmount)); return{gross_amount,percent_discount_amount,rupee_discount_amount,total_discount_amount,net_amount,sales_return_amount,received_amount,total_amount,balance_amount:Math.max(0,total_amount-received_amount),return_amount:Math.max(0,received_amount-total_amount)}; };

function InvoiceItemModal({isOpen,onClose,onSubmit,article=null,inventory=[]}) {
  const [selectedArticleNo,setSelectedArticleNo]=useState(""); const [row,setRow]=useState(null); const [error,setError]=useState("");
  useEffect(()=>{if(!isOpen)return;setError("");if(article){setSelectedArticleNo(article.article_no);setRow(article);}else{setSelectedArticleNo("");setRow(null);}},[article,inventory,isOpen]);
  const selectedArticle=inventory.find((item)=>item.article_no===selectedArticleNo);
  const chooseArticle=(articleNo)=>{const match=inventory.find((item)=>item.article_no===articleNo);setSelectedArticleNo(articleNo);setRow(match?newInvoiceArticle(match):null);};
  const update=(field,value)=>setRow((prev)=>{if(!prev)return prev;if(["quantity_pkt","dzn","pcs"].includes(field)){const next=syncQuantity(prev,field,value),available=numberValue(selectedArticle?.stock_pcs);if(numberValue(next.pcs)>available){setError(`Only ${available} pieces are available.`);return syncQuantity(prev,"pcs",String(available));}setError("");return next;}return{...prev,[field]:value};});
  const submit=(event)=>{event.preventDefault();if(!row?.article_no)return setError("Select an article first.");if(numberValue(row.pcs)<=0)return setError("Enter quantity.");if(numberValue(row.pcs)>numberValue(selectedArticle?.stock_pcs))return setError(`Only ${numberValue(selectedArticle?.stock_pcs)} pieces are available.`);onSubmit(calculateArticle(row));};
  return <Modal isOpen={isOpen} onClose={onClose} maxWidth="max-w-3xl" title={article?"Edit Invoice Item":"Add Invoice Item"} subtitle="Select the item and enter its sale quantity" footer={<div className="flex w-full items-center justify-between gap-3"><p className="min-w-0 flex-1 text-xs font-medium text-red-600">{error}</p><div className="flex gap-3"><Button outline variant="secondary" onClick={onClose}>Discard</Button><Button icon={Save} onClick={submit}>{article?"Save Item":"Add Item"}</Button></div></div>}>
    <form onSubmit={submit} className="grid grid-cols-1 gap-3.5 p-0.5 md:grid-cols-3">
      <div className="md:col-span-3"><Select label="Search & Select Article" value={selectedArticleNo} onChange={chooseArticle} options={inventory} placeholder="Choose article by number or description" />{selectedArticle&&<div className="mt-2 grid gap-2 rounded-xl border border-gray-300 bg-gray-50 p-3 text-xs text-gray-600 md:grid-cols-3"><p><b className="text-gray-800">Article No:</b> {selectedArticle.article_no}</p><p><b className="text-gray-800">Available:</b> {selectedArticle.stock_pcs||0} pcs ({selectedArticle.unit?(selectedArticle.stock_pcs/selectedArticle.unit).toFixed(2):0} pkt)</p><p><b className="text-gray-800">Sale Rate:</b> {Number(selectedArticle.sale_rate||selectedArticle.rate||0).toFixed(2)}</p></div>}</div>
      <div className="md:col-span-3 rounded-xl border border-sky-200 bg-sky-50 px-3 py-2 text-xs text-sky-800">Type quantity in any one box. Packets, dozens and pieces calculate automatically.</div>
      <Input label="Packets" type="number" min="0" max={selectedArticle?.unit?selectedArticle.stock_pcs/selectedArticle.unit:0} value={row?.quantity_pkt||""} onChange={(e)=>update("quantity_pkt",e.target.value)} placeholder="e.g. 2" />
      <Input label="Dozens" type="number" min="0" max={(selectedArticle?.stock_pcs||0)/12} value={row?.dzn||""} onChange={(e)=>update("dzn",e.target.value)} placeholder="Auto calculated" />
      <Input label="Pieces" type="number" min="0" max={selectedArticle?.stock_pcs||0} value={row?.pcs||""} onChange={(e)=>update("pcs",e.target.value)} placeholder="Auto calculated" />
      <Input label="Sale Rate" type="number" min="0" value={row?.rate||""} onChange={(e)=>update("rate",e.target.value)} placeholder="0.00" />
      <Input label="Discount" value={row?.discount||""} onChange={(e)=>update("discount",e.target.value)} placeholder="10% or 50" required={false} />
      <Input label="Amount" value={row?calculateArticle(row).amount.toFixed(2):"0.00"} disabled />
      <button type="submit" className="hidden" />
    </form>
  </Modal>;
}

export default function InvoiceFormModal({isOpen,onClose,onAction}) {
  const {showToast}=useToast();
  const [step,setStep]=useState("entry"),[entryStage,setEntryStage]=useState(1),[customerId,setCustomerId]=useState(""),[customerMode,setCustomerMode]=useState("registered"),[quickCustomerOpen,setQuickCustomerOpen]=useState(false),[walkIn,setWalkIn]=useState({customer_name:"Walk-in Customer",person_name:"",urdu_title:"",phone_number:"",address:"",city:""}),[customers,setCustomers]=useState([]),[salesmanName,setSalesmanName]=useState(""),[articles,setArticles]=useState([]),[inventory,setInventory]=useState([]),[itemModal,setItemModal]=useState({isOpen:false,article:null}),[scanModalOpen,setScanModalOpen]=useState(false),[salesReturnRows,setSalesReturnRows]=useState([]),[includeSalesReturn,setIncludeSalesReturn]=useState(false),[returnAdjustment,setReturnAdjustment]=useState({type:"none",value:""}),[payment,setPayment]=useState({received_now:false,amount:"",method:"cash"}),[invoiceNumber,setInvoiceNumber]=useState(""),[submitting,setSubmitting]=useState(false),[loadingData,setLoadingData]=useState(false),[error,setError]=useState("");
  const [salesReturnInventory,setSalesReturnInventory]=useState([]),[salesReturnLoading,setSalesReturnLoading]=useState(false);

  useEffect(()=>{if(!isOpen)return;let cancelled=false;setStep("entry");setEntryStage(1);setCustomerId("");setCustomerMode("registered");setQuickCustomerOpen(false);setWalkIn({customer_name:"Walk-in Customer",person_name:"",urdu_title:"",phone_number:"",address:"",city:""});setSalesmanName("");setArticles([]);setSalesReturnRows([]);setIncludeSalesReturn(false);setReturnAdjustment({type:"none",value:""});setPayment({received_now:false,amount:"",method:"cash"});setError("");setLoadingData(true);Promise.all([fetchCustomers(),fetchInventory(),fetchMyInvoiceCounter({year:new Date().getFullYear()})]).then(([customerRows,inventoryRows,counter])=>{if(cancelled)return;setCustomers((Array.isArray(customerRows)?customerRows:[]).filter((c)=>c.isActive!==false));setInventory(toInvoiceInventory(Array.isArray(inventoryRows)?inventoryRows:[]));setInvoiceNumber(formatInvoiceNumber(counter?.year||new Date().getFullYear(),counter?.next_invoice_no||1));}).catch((err)=>{if(!cancelled){setCustomers([]);setInventory([]);setError(err?.response?.data?.message||"Could not load invoice data from the server.");}}).finally(()=>{if(!cancelled)setLoadingData(false);});return()=>{cancelled=true;};},[isOpen]);
  useEffect(()=>{let cancelled=false;if(!customerId||customerMode!=="registered"){setSalesReturnInventory([]);return;}setSalesReturnLoading(true);fetchSalesReturnable(customerId).then((items)=>{if(!cancelled)setSalesReturnInventory(Array.isArray(items)?items:[]);}).catch((err)=>{if(!cancelled){setSalesReturnInventory([]);setError(err?.response?.data?.message||"Could not load returnable articles for this customer.");}}).finally(()=>{if(!cancelled)setSalesReturnLoading(false);});return()=>{cancelled=true;};},[customerId,customerMode]);

  const calculatedArticles=useMemo(()=>articles.map(calculateArticle),[articles]);
  const itemInventory=useMemo(()=>inventory.map((item)=>{const used=articles.reduce((s,r)=>r.article_no===item.article_no&&r._key!==itemModal.article?._key?s+numberValue(r.pcs):s,0),stock=Math.max(0,numberValue(item.stock_pcs)-used);return{...item,stock_pcs:stock,label:`${item.article_no} | ${item.description||"Article"} | Available ${stock} pcs | Sale ${item.sale_rate||item.rate||0}`};}).filter((item)=>item.stock_pcs>0||item.article_no===itemModal.article?.article_no),[articles,inventory,itemModal.article]);
  const scanInventory=useMemo(()=>inventory.map((item)=>({...item,stock_pcs:Math.max(0,numberValue(item.stock_pcs)-articles.reduce((s,r)=>r.article_no===item.article_no?s+numberValue(r.pcs):s,0))})).filter((item)=>item.stock_pcs>0),[articles,inventory]);
  const salesReturnTotals=useMemo(()=>returnTotals(salesReturnRows,returnAdjustment),[salesReturnRows,returnAdjustment]);
  const invoiceTotals=useMemo(()=>calculateInvoiceTotals(calculatedArticles,salesReturnTotals.amount,payment.received_now?payment.amount:0),[calculatedArticles,salesReturnTotals.amount,payment]);
  const selectedCustomer=useMemo(()=>customerMode==="walkin"?{_id:"",...walkIn,customer_name:walkIn.customer_name.trim()||"Walk-in Customer",isWalkIn:true}:customers.find((c)=>c._id===customerId)||null,[customerId,customerMode,customers,walkIn]);
  const customerOptions=useMemo(()=>customers.map((c)=>({label:c.customer_name,value:c._id})),[customers]);
  const draftInvoice=useMemo(()=>({invoice_number:invoiceNumber,invoice_date:todayInput(),customer_id:selectedCustomer?._id||"",customer_name:selectedCustomer?.customer_name||"",customer_urdu_title:selectedCustomer?.urdu_title||"",salesman_name:salesmanName.trim(),customer_phone:selectedCustomer?.phone_number||"",customer_address:selectedCustomer?.address||"",articles:calculatedArticles,sales_return:{articles:salesReturnRows,adjustment:returnAdjustment,total_pcs:salesReturnTotals.pcs,gross_amount:salesReturnTotals.gross,adjustment_amount:salesReturnTotals.adjustment,total_amount:salesReturnTotals.amount},payment:{...payment,amount:payment.received_now?numberValue(payment.amount):0},...invoiceTotals}),[invoiceNumber,selectedCustomer,salesmanName,calculatedArticles,salesReturnRows,returnAdjustment,salesReturnTotals,payment,invoiceTotals]);

  const handleQuickCustomerCreate=async(payload)=>{try{const created=await createCustomer(payload);const rows=await fetchCustomers();setCustomers((Array.isArray(rows)?rows:[]).filter((c)=>c.isActive!==false));setCustomerMode("registered");setCustomerId(created._id);setQuickCustomerOpen(false);showToast({type:"success",message:`${created.customer_name} added and selected.`});return created;}catch(err){const message=err?.response?.data?.message||"Failed to add customer";showToast({type:"error",message});throw err;}};
  const upsertArticle=(article)=>{setArticles((rows)=>{const edited=rows.findIndex((r)=>r._key===article._key);if(edited>=0)return rows.map((r)=>r._key===article._key?article:r);const duplicate=rows.findIndex((r)=>r.article_no===article.article_no);if(duplicate>=0)return rows.map((r,i)=>i===duplicate?calculateArticle(syncQuantity(r,"pcs",String(numberValue(r.pcs)+numberValue(article.pcs)))):r);return[...rows,article];});setItemModal({isOpen:false,article:null});};
  const mergeScannedRows=(incoming)=>{setArticles((current)=>{const next=[...current];incoming.forEach((row)=>{const index=next.findIndex((item)=>item.article_no===row.article_no);if(index>=0)next[index]=calculateArticle(syncQuantity(next[index],"pcs",String(numberValue(next[index].pcs)+numberValue(row.pcs))));else next.push(row);});return next;});setScanModalOpen(false);};
  const removeRow=(key)=>setArticles((rows)=>rows.filter((r)=>r._key!==key));
  const advance=()=>{if(entryStage===1&&!selectedCustomer)return setError("Select a customer to continue.");if(entryStage===1&&!salesmanName.trim())return setError("Enter the salesperson name to continue.");if(entryStage===2&&!calculatedArticles.length)return setError("Add at least one article to continue.");setError("");setEntryStage((s)=>Math.min(3,s+1));};
  const review=()=>{if(!selectedCustomer)return setError("Select a customer.");if(!salesmanName.trim())return setError("Salesman name is required.");if(!calculatedArticles.length)return setError("Add at least one article.");const over=calculatedArticles.find((r)=>numberValue(r.pcs)>numberValue(inventory.find((i)=>i.article_no===r.article_no)?.stock_pcs));if(over)return setError(`${over.article_no}: only ${numberValue(inventory.find((i)=>i.article_no===over.article_no)?.stock_pcs)} pieces are available.`);setError("");setStep("preview");};
  const save=async()=>{setSubmitting(true);try{await onAction(draftInvoice);onClose();}catch(err){const message=err?.response?.data?.message||"Failed to save invoice";setError(message);showToast({type:"error",message});}finally{setSubmitting(false);}};

  return <Modal isOpen={isOpen} onClose={onClose} title={step==="entry"?"New Sales Invoice":"Review Sales Invoice"} subtitle={step==="entry"?`Invoice ${invoiceNumber||"..."} · Complete the 3 simple sections below`:"Check customer, items and payment before saving"} maxWidth="max-w-5xl" closeOnEscape={!quickCustomerOpen} footer={<div className="flex w-full flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><p className="min-h-4 text-xs font-medium text-red-600">{error}</p><div className="flex w-full gap-2 sm:w-auto sm:gap-3">{step==="preview"?<><Button variant="secondary" outline icon={ArrowLeft} onClick={()=>setStep("entry")} disabled={submitting}>Back</Button><Button className="flex-1 sm:flex-none" icon={Save} onClick={save} loading={submitting}>Save Invoice</Button></>:<>{entryStage>1&&<Button className="flex-1 sm:flex-none" variant="secondary" outline icon={ArrowLeft} onClick={()=>{setError("");setEntryStage((s)=>s-1);}}>Back</Button>}{entryStage<3?<Button className="flex-1 sm:flex-none" icon={ArrowRight} iconPosition="right" onClick={advance} disabled={loadingData}>{entryStage===1?"Continue to Items":"Continue to Payment"}</Button>:<Button className="flex-1 sm:flex-none" icon={ArrowRight} iconPosition="right" onClick={review} disabled={loadingData}>Review Invoice</Button>}</>}</div></div>}>
    {step==="preview"?<InvoicePrintPreview invoice={draftInvoice} businessName="Akhlaq Garments" printMode="a5"/>:<div className="grid gap-5 p-0.5">
      <div className="grid grid-cols-3 overflow-hidden rounded-xl border border-gray-200 bg-gray-50 text-center text-[11px] font-semibold sm:text-xs"><button type="button" onClick={()=>setEntryStage(1)} className={`border-r border-gray-200 px-2 py-2.5 ${entryStage===1?"bg-teal-50 text-teal-700":"bg-white text-gray-700"}`}>1 · Customer</button><button type="button" disabled={entryStage<2} onClick={()=>setEntryStage(2)} className={`border-r border-gray-200 px-2 py-2.5 ${entryStage===2?"bg-teal-50 text-teal-700":"text-gray-400"}`}>2 · Items</button><button type="button" disabled={entryStage<3} onClick={()=>setEntryStage(3)} className={`px-2 py-2.5 ${entryStage===3?"bg-teal-50 text-teal-700":"text-gray-400"}`}>3 · Payment</button></div>
      {loadingData&&<p className="rounded-xl border border-gray-200 bg-gray-50 p-3 text-sm text-gray-500">Loading customers and inventory from server...</p>}
      <section className={entryStage===1?"block":"hidden"}><SectionHeader step="1" title="Who is buying?" subtitle="Select the customer and salesperson handling this sale"/><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><div className="relative grid w-full grid-cols-2 rounded-xl border border-gray-300 bg-gray-100 p-1 sm:w-[270px]"><motion.span animate={{x:customerMode==="walkin"?"100%":"0%"}} transition={{type:"spring",stiffness:500,damping:36}} className="absolute bottom-1 left-1 top-1 w-[calc(50%-4px)] rounded-lg bg-white shadow-sm ring-1 ring-gray-200"/><button type="button" onClick={()=>{setCustomerMode("registered");setError("");}} className={`relative z-10 rounded-lg px-3 py-2 text-xs font-semibold ${customerMode==="registered"?"text-gray-950":"text-gray-500"}`}>Registered</button><button type="button" onClick={()=>{setCustomerMode("walkin");setCustomerId("");setIncludeSalesReturn(false);setSalesReturnRows([]);setError("");}} className={`relative z-10 rounded-lg px-3 py-2 text-xs font-semibold ${customerMode==="walkin"?"text-gray-950":"text-gray-500"}`}>Walk-in</button></div>{customerMode==="registered"&&<Button size="sm" outline icon={Plus} onClick={()=>setQuickCustomerOpen(true)}>Quick Add Customer</Button>}</div><div className="grid grid-cols-1 gap-3.5 md:grid-cols-2">{customerMode==="registered"?<Select label="Customer *" value={customerId} onChange={setCustomerId} options={customerOptions} placeholder="Choose customer"/>:<Input label="Customer Name" value={walkIn.customer_name} onChange={(e)=>setWalkIn((w)=>({...w,customer_name:e.target.value}))} required={false}/>}<Input label="Salesperson *" value={salesmanName} onChange={(e)=>setSalesmanName(capitalizeWords(e.target.value))} placeholder="Who made this sale?" required={false}/></div>{customerMode==="walkin"&&<div className="mt-3 grid grid-cols-1 gap-3.5 rounded-xl border border-gray-200 bg-gray-50 p-3 md:grid-cols-3"><Input label="Phone" value={walkIn.phone_number} onChange={(e)=>setWalkIn((w)=>({...w,phone_number:e.target.value}))} required={false}/><Input label="Person Name" value={walkIn.person_name} onChange={(e)=>setWalkIn((w)=>({...w,person_name:e.target.value}))} required={false}/><Input label="Address" value={walkIn.address} onChange={(e)=>setWalkIn((w)=>({...w,address:e.target.value}))} required={false}/></div>}{selectedCustomer&&customerMode==="registered"&&<div className="mt-3 grid gap-2 rounded-xl border border-gray-300 bg-gray-50 p-3 text-xs text-gray-600 md:grid-cols-4"><p><b className="text-gray-800">Person:</b> {selectedCustomer.person_name||"-"}</p><p lang="ur"><b className="text-gray-800">Urdu:</b> {selectedCustomer.urdu_title||"-"}</p><p><b className="text-gray-800">Phone:</b> {selectedCustomer.phone_number||"-"}</p><p><b className="text-gray-800">City:</b> {selectedCustomer.city||"-"}</p></div>}</section>
      <section className={entryStage===2?"block":"hidden"}><SectionHeader step="2" title="Invoice Items" subtitle={`${calculatedArticles.length} item${calculatedArticles.length===1?"":"s"} added`} right={<div className="flex gap-2"><Button size="sm" outline icon={ScanLine} onClick={async()=>{await unlockScanAudio().catch(()=>false);setScanModalOpen(true);}}>Scan QR</Button><Button size="sm" icon={Plus} onClick={()=>setItemModal({isOpen:true,article:null})}>Add Item</Button></div>}/><div className="grid gap-2 md:hidden">{calculatedArticles.length===0?<div className="rounded-xl border border-dashed border-gray-300 px-4 py-8 text-center"><p className="text-sm font-semibold text-gray-700">No sale item added</p><p className="mt-1 text-xs text-gray-400">Tap Add Item, or scan the packet label.</p></div>:calculatedArticles.map((a)=><div key={a._key} className="rounded-xl border border-gray-200 bg-white p-3 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold text-gray-900">{a.description||a.article_no}</p><p className="text-xs font-medium text-teal-700">{a.article_no}</p></div><div className="flex gap-1"><button type="button" onClick={()=>setItemModal({isOpen:true,article:a})} className="rounded-lg p-2 text-gray-500 hover:bg-gray-100"><Edit3 className="h-4 w-4"/></button><button type="button" onClick={()=>removeRow(a._key)} className="rounded-lg p-2 text-red-500 hover:bg-red-50"><Trash2 className="h-4 w-4"/></button></div></div><div className="mt-3 grid grid-cols-4 gap-2 border-t border-gray-100 pt-3 text-xs"><div><span className="text-gray-400">Packets</span><p className="font-semibold">{a.quantity_pkt||0}</p></div><div><span className="text-gray-400">Pieces</span><p className="font-semibold">{a.pcs||0}</p></div><div><span className="text-gray-400">Rate</span><p className="font-semibold">{a.rate.toFixed(2)}</p></div><div className="text-right"><span className="text-gray-400">Amount</span><p className="font-bold text-emerald-700">{a.amount.toFixed(2)}</p></div></div></div>)}</div><div className="hidden overflow-x-auto rounded-xl border border-gray-300 md:block"><table className="w-full min-w-[920px] border-collapse"><thead><tr className="border-b border-gray-300 bg-gray-50 text-xs font-semibold text-gray-500"><th className="w-10 px-2 py-2.5">#</th><th className="px-2 py-2.5 text-left">Article No</th><th className="px-2 py-2.5 text-left">Description</th><th className="px-2 py-2.5">Size</th><th className="px-2 py-2.5">Pckt</th><th className="px-2 py-2.5">Dzn</th><th className="px-2 py-2.5">Pieces</th><th className="px-2 py-2.5">Sale Rate</th><th className="px-2 py-2.5">Discount</th><th className="px-3 py-2.5 text-right">Amount</th><th className="w-24 px-2 py-2.5">Actions</th></tr></thead><tbody>{calculatedArticles.length===0?<tr><td colSpan={11} className="px-7 py-10 text-center text-sm text-gray-400">No items added yet. Add an article manually or scan its QR label.</td></tr>:calculatedArticles.map((a,index)=><tr key={a._key} className="border-b border-gray-200 hover:bg-emerald-50/30"><td className="px-2 py-3 text-center text-xs text-gray-400">{index+1}</td><td className="px-2 py-3 text-sm font-semibold">{a.article_no}</td><td className="px-2 py-3 text-sm text-gray-600">{a.description}</td><td className="px-2 py-3 text-center text-sm">{a.size||"-"}</td><td className="px-2 py-3 text-center text-sm">{a.quantity_pkt||0}</td><td className="px-2 py-3 text-center text-sm">{a.dzn||0}</td><td className="px-2 py-3 text-center text-sm">{a.pcs||0}</td><td className="px-2 py-3 text-center text-sm">{a.rate.toFixed(2)}</td><td className="px-2 py-3 text-center text-sm">{a.discount||"-"}</td><td className="px-3 py-3 text-right text-sm font-semibold text-emerald-700">{a.amount.toFixed(2)}</td><td className="px-2 py-3"><div className="flex justify-end gap-1"><button type="button" onClick={()=>setItemModal({isOpen:true,article:a})} className="rounded-lg p-1.5 text-gray-400 hover:bg-gray-100"><Edit3 className="h-4 w-4"/></button><button type="button" onClick={()=>removeRow(a._key)} className="rounded-lg p-1.5 text-gray-400 hover:bg-red-50 hover:text-red-500"><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody><tfoot><tr className="bg-gray-50 font-semibold"><td colSpan={9} className="px-4 py-3 text-right text-sm text-gray-600">Total</td><td className="px-3 py-3 text-right text-sm text-emerald-700">{invoiceTotals.net_amount.toFixed(2)}</td><td/></tr></tfoot></table></div></section>
      <section className={entryStage===3?"block":"hidden"}><SectionHeader step="3" title="Return & Payment" subtitle="Record any sales return and the payment received with this invoice"/><div className="grid gap-5"><div className="overflow-hidden rounded-xl border border-gray-300 bg-white"><div className="flex items-center justify-between gap-3 bg-gray-50 px-4 py-3"><div className="flex min-w-0 items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-red-50 text-red-600"><RotateCcw className="h-4 w-4"/></div><div><p className="text-sm font-semibold text-gray-800">Sales Return with this Invoice</p><p className="text-xs text-gray-400">Optional — return previously sold goods and adjust payable.</p></div></div>{customerMode==="registered"?<Checkbox checked={includeSalesReturn} onChange={(checked)=>{setIncludeSalesReturn(checked);if(!checked){setSalesReturnRows([]);setReturnAdjustment({type:"none",value:""});}}} label="Add return"/>:<span className="text-xs font-medium text-gray-400">Not available for walk-in sale</span>}</div>{customerMode==="registered"&&includeSalesReturn&&<div className="border-t border-gray-200 p-4"><ReturnEditor title="Returned Articles" subtitle={salesReturnLoading?"Loading customer sold articles...":"Customer sold articles"} inventory={salesReturnInventory} rows={salesReturnRows} onChange={setSalesReturnRows} adjustment={returnAdjustment} onAdjustmentChange={setReturnAdjustment}/></div>}</div><div className="border-t border-gray-200 pt-4"><SectionHeader title="Payment Received" subtitle="Record payment received against the final payable amount"/><PaymentEditor payment={payment} onChange={setPayment}/></div><div className="grid grid-cols-2 gap-2 rounded-xl border border-gray-300 bg-gray-50 p-3 text-sm md:grid-cols-6"><div><span className="text-gray-500">Sale</span><p className="font-semibold">{invoiceTotals.net_amount.toFixed(2)}</p></div><div><span className="text-gray-500">Return PCs</span><p className="font-semibold">{salesReturnTotals.pcs}</p></div><div><span className="text-gray-500">Return</span><p className="font-semibold text-red-600">-{salesReturnTotals.amount.toFixed(2)}</p></div><div><span className="text-gray-500">Payable</span><p className="font-semibold text-emerald-700">{invoiceTotals.total_amount.toFixed(2)}</p></div><div><span className="text-gray-500">Received</span><p className="font-semibold text-sky-700">{invoiceTotals.received_amount.toFixed(2)}</p></div><div className="rounded-lg bg-white px-3 py-2 ring-1 ring-gray-200"><span className="text-gray-500">{invoiceTotals.return_amount>0?"Change / Advance":"Balance Due"}</span><p className={`text-base font-bold ${invoiceTotals.return_amount>0?"text-emerald-700":"text-red-600"}`}>{(invoiceTotals.return_amount||invoiceTotals.balance_amount).toFixed(2)}</p></div></div></div></section>
    </div>}
    <InvoiceItemModal isOpen={itemModal.isOpen} article={itemModal.article} inventory={itemInventory} onClose={()=>setItemModal({isOpen:false,article:null})} onSubmit={upsertArticle}/><InvoiceScanModal isOpen={scanModalOpen} inventory={scanInventory} onClose={()=>setScanModalOpen(false)} onApply={mergeScannedRows}/><CustomerFormModal isOpen={quickCustomerOpen} onClose={()=>setQuickCustomerOpen(false)} onSubmit={handleQuickCustomerCreate}/>
  </Modal>;
}
